// test.js
const nodevisionDB = require('./build/Release/nodevision_db');
console.log('NodevisionDB module loaded:', nodevisionDB);

// If you exposed a function, for example:
// console.log(nodevisionDB.createGraphDB());
